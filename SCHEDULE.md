# 出題設定の運用

## コードの構成

- `src/lib/scheduleTypes.ts`：設定の型と各項目の説明。旧方式と新方式を区別します。
- `src/lib/legacySchedule.ts`：移行前の固定順を再現する互換処理。
- `src/lib/schedule.ts`：有効な設定の選択と、新方式のシャッフル・巡回計算。
- `src/lib/words.ts`：ページ読込時にブラウザで答えと通算問題番号を計算し、画面で共有します。開きっぱなしの画面では再計算しません。

この整理による設定ファイル形式・出題順・操作手順の変更はありません。

## 指定日以降の出題一覧

保存済みの最新設定を基に、指定日（当日を含む）から、その日が属する巡回の最終日までをCSV形式で表示します。駅名はゲームの回答に使う5文字の表記です。

```sh
npm run schedule:list -- 2026-10-01
```

ファイルへ保存する場合（UTF-8 BOM付き、既存ファイルは上書きしません）：

```sh
npm run schedule:list -- 2026-10-01 --output schedule-list.csv
```

標準出力をリダイレクトする場合は、npmの案内文が混ざらないよう `npm run --silent schedule:list -- 2026-10-01 > schedule-list.csv` とします。

指定日は端末時刻の変更なしで指定できます。期間中に適用される設定変更も反映し、次の巡回の初日は含めません。現在の巡回を確認するときはその巡回内の日付を指定してください。駅データを編集しただけの未反映の変更は含まれず、先に `schedule:update` を実行する必要があります。

この操作は出題設定・基準日を変更しません。出力ファイルは管理者向けの答え一覧なので、公開用の `public` や `src` には保存せず、コミットにも含めないでください。

## 保存方式

2026年10月1日以降の並べ替え済み出題リストは保存・配信しません。候補駅一覧（文字コード順）、シード、直前の駅などの計算条件を保存し、ブラウザで出題順を再現します。リンクのみの更新では元の計算条件と進行位置を引き継ぎます。

初回適用前の互換性のため、従来の出題順は9月30日までの分だけ配信します。初回適用日の変更に使う従来の完全な順序は `scripts/legacy-order.json` に保存し、ブラウザ用コードから参照しません。公開するのは通常どおり `build` の内容です。

駅情報の記載順にも従来の出題順が残らないよう、配信する設定内の駅情報も駅名順で保存します。シードと計算処理は公開されるため、将来の答えを計算して取得することは可能です。

## 初回設定（生成済み）

- 初回適用日：2026-10-01（端末のローカル日付）
- 9月30日までは既存の出題順、10月1日から現在の巡回の未出題駅を再シャッフル。
- 全体シャッフル日は2025/11/25を維持。次の巡回から開始日を自動表示。
- 駅名・読みの基準日は9月30日まで2026年4月2日、10月1日から設定生成日の2026年9月22日。
- 今回の巡回の残りは1040駅。駅変更がなければ次の全体シャッフルは2029-08-06。

## 動作確認

```sh
npm run test:schedule
npm run build
npm start
```

http://localhost:3000 で確認します。問題はページ読込時に確定し、開きっぱなしで日付をまたいでも切り替わりません。

## 駅の追加・削除

1. 最新の公開済みコード・出題設定を使います。
2. `src/constants/station_names_5_katakana.ts` の駅データを編集します。駅名はアイウエオ順に並べています。記載順は今後の出題順に影響しません。
3. 未来の適用日を指定します（以下の日付は例です）。

```sh
npm run schedule:update -- 2026-10-01
```

4. 追加・削除駅、残りの出題数、次の巡回開始日、基準日を確認します。
5. テスト・ビルドし、駅データと `src/constants/schedule.json` を一緒にコミット・デプロイします。

初回設定の適用前に内容や日付を変更する場合は、次のようにします。

```sh
npm run schedule:update -- 2026-10-01 --init --replace-pending
```

`--replace-pending` は未適用の最終設定を置き換えます。すでに配信した設定の変更は利用者への影響を確認して行ってください。初回以外は `--init` を付けません。同じ入力・適用日で再実行しても順序は変わりませんが、基準日は実行日に更新します。

## 変更の扱い

- 追加駅を未出題駅と一緒にシャッフルし、末尾には固定しません。
- 出題済みの駅を削除しても過去の日付・出題は維持します。
- 未出題の駅の削除は残りの候補から除外します。
- 再開駅は、削除・再追加の履歴を使って新設扱いにします。同じ読みが巡回内で出題済みでも候補へ戻ります。
- リンクだけの変更は順序を変更しません。駅情報も指定した適用日から切り替わります。
- 駅名・読みの基準日は処理実行環境のローカル日付です。駅データと同じ適用日から表示します。適用日前は旧データと旧基準日を表示します。基準日だけを更新する場合も、指定した適用日から切り替わり、出題順は維持します。Wikipediaの自動照合はしません。
- 全体シャッフルの前後で同じ駅が連続する場合、次の先頭と2番目を交換します。実現に必要なため有効駅は2駅以上とします。
- 駅数が変われば次の巡回開始日も変わります。残りがゼロなら適用日から次の巡回です。
- 通常のビルドだけでは設定・基準日を更新しません。

## 公開上の注意

適用日前に余裕を持って公開してください。利用者は各自のローカル日付で切り替わります。開きっぱなしのページや古いキャッシュには強制反映しません。適用日に間に合わない初回公開は、旧版が配信されている間の実際の出題を踏まえた再移行が必要なので、そのまま公開しないでください。

設定ファイルは履歴を含めて保存してください。手動のdayOffset調整や全体シャッフルは不要です。未来の答えの秘匿を目的とする仕組みではありません。

基準日は各出題設定に保存します。開きっぱなしの画面では、問題・駅データとともに読込時点の基準日を維持します。
